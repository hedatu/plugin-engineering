{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": [
    "stage",
    "status",
    "run_id",
    "candidates_seen",
    "build_ready_count",
    "research_more_count",
    "skip_count",
    "top_ranked_opportunities",
    "next_step",
    "redaction_checks"
  ],
  "properties": {
    "stage": { "type": "string", "enum": ["STRATEGY_V2_CANDIDATE_SCORES"] },
    "status": { "type": "string", "enum": ["passed", "skipped", "failed"] },
    "run_id": { "type": "string" },
    "candidates_seen": { "type": "integer" },
    "build_ready_count": { "type": "integer" },
    "research_more_count": { "type": "integer" },
    "skip_count": { "type": "integer" },
    "top_ranked_opportunities": { "type": "array", "items": { "type": "object" } },
    "next_step": { "type": "string" },
    "redaction_checks": { "type": "object" }
  }
}
