# Product Catalog

This package owns the local `state/product_catalog.json` contract for plugin site generation, payment-site routing, and multi-product storefront previews.

Current defaults:

- Seeds `LeadFill One Profile`
- Keeps `chatgpt2obsidian` marked as legacy test-only memory, not the production key for LeadFill
- Uses only public or placeholder payment-site values
